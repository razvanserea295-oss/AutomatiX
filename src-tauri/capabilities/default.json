{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Automatix window. The app is a thin shell that loads its UI from the production server, so window-control + updater IPC must be granted to that remote origin too.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://app.automatix.online", "https://*.automatix.online"]
  },
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized"
  ]
}
