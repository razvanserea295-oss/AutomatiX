# Debug log — Promix (2026-06-16)

Systematic backend/logic debugging pass. Scope: `electron/services`, `electron/ipc`, `server`, `src/pages` (logic), `src/lib`, `src/store`, `src/hooks` (non-UI). **Out of scope** (other sessions): UI primitives / `theme.css` / shell / Login/Boot/AppBackground; and the Fix-IPC topics (money-float, migration 110, PDF font).

## Status table

| # | Fișier:linie | Severitate | Bug | Status |
|---|--------------|-----------|-----|--------|
| 1 | `electron/services/reportsService.ts:228` | 🟠 important | `listPresets` făcea `JSON.parse(r.config)` neguardat în `.map` — un singur preset cu `config` corupt arunca → **lista întreagă de rapoarte salvate dădea 500**. | ✅ reparat |
| 2 | `electron/services/rfqService.ts:256` | 🟠 important | `create` scria în `rfqs` + `rfq_items` + `rfq_invitations` **fără tranzacție** → eșec la o linie/invitație lăsa un RFQ orfan/parțial. | ✅ reparat (BEGIN/COMMIT/ROLLBACK) |
| 3 | `electron/services/quotationService.ts:307` | 🟠 important | `create` scria `quotations` + `quotation_lines` fără tranzacție → ofertă orfană cu linii parțiale dar totaluri nenule. | ✅ reparat |
| 4 | `electron/services/contractService.ts:128` | 🟠 important | `createContract` scria `contracts` + `contract_sections` (seed) fără tranzacție → contract fără secțiuni la eșec parțial. | ✅ reparat |
| 5 | `electron/services/procurementService.ts:282` | 🟠 important | `createPurchaseOrder` scria `purchase_orders` + `purchase_order_lines` fără tranzacție → comandă goală/parțială. | ✅ reparat |

| 6 | `electron/services/quotationService.ts:369` | 🟠 important | `update` făcea UPDATE header → `DELETE` toate liniile → re-`INSERT`, fără tranzacție → un re-insert eșuat **ștergea TOATE liniile** unei oferte salvate (pierdere de date), păstrând totalurile actualizate. | ✅ reparat (tranzacție pe tot update-ul) |

**Notă tranzacții (#2–6):** același pattern de atomicitate ca goods receipt (audit 2026-06-11): `db.run('BEGIN')` → scrieri → `COMMIT`, cu `ROLLBACK` în catch. sql.js e sincron/single-thread (fără concurență reală la nivel JS), dar tranzacția garantează **atomicitatea** unui write multi-tabel — fără ea, o eroare la mijloc (constraint, FK, valoare invalidă) lăsa entitatea-părinte fără copii (sau, la #6, fără copii deloc).

## Test E2E sandbox (sql.js)

`electron/services/quotationService.tx.test.ts` — driver pe **QuotationService real**, DB sql.js throwaway cu `quotation_lines CHECK(unit_price >= 0)`:
1. `create` commit header + linii la succes ✅
2. `create` cu o linie invalidă → throw + **0 oferte** (rollback, fără orfan) ✅
3. `update` cu linie nouă invalidă → throw + linia originală **intactă** (fără pierdere de date) ✅

`npx vitest --run electron/services/quotationService.tx.test.ts` → 3/3 passed. (Aceeași mecanică BEGIN/COMMIT/ROLLBACK la #2/#4/#5 — verificată prin review + mecanism identic.)

## Findings raportate (arhitecturale — NU reparate, ca să nu fac churn / out-of-scope)

- **ℹ️ Error UX la constraint violations:** în `server/index.ts` (~638), erorile non-`CommandError` (ex. `UNIQUE`/`FK` din sql.js) devin generic `500 "Eroare internă."` — sigur (fără leak de SQL) dar neutil. Ideal: serviciile pre-validează și aruncă `CommandError.conflict(...)` prietenos (multe deja o fac, ex. contract duplicat). De mapat caz cu caz, nu global.
- **ℹ️ RBAC defense-in-depth:** `electron/ipc/broadcasts.ts` + `demoSeed.ts` înregistrează comenzi admin cu `withAuthenticatedUser`, dar service-ul **impune** admin intern (`requireAdmin`/`ensureAdmin`) → **NU e gaură de securitate** (non-adminul e respins server-side). Recomandare cosmetică: mută gardul pe `withAdminUser` la boundary.

## Batch 2 — rezolvare raportate (A+B) + audit gaps (E)

### A. Error UX — mapare constraint SQLite → status prietenos ✅
- Nou: `server/sqliteErrors.ts` (`mapSqliteConstraintError`) — modul pur, testabil, fără side-effects.
- `server/index.ts` (error branch din ruta `/api/cmd/:command`) folosește maparea înainte de fallback-ul generic:
  - **UNIQUE** → `409` „Înregistrarea există deja (câmp duplicat)."
  - **FOREIGN KEY** → `422` „...folosită de altă înregistrare."
  - **NOT NULL** → `422` „Câmp obligatoriu lipsește: <câmp>."
  - **CHECK** → `422` „Valoare invalidă (regula: ...)."
  - rest → `500 "Eroare internă."` (fără leak SQL).
- Test: `server/sqliteErrors.test.ts` — 5/5, violări sql.js REALE (UNIQUE/NOTNULL/CHECK/FK + non-constraint→null).

### B. RBAC defense-in-depth — gate la boundary ✅
- `electron/ipc/broadcasts.ts`: `admin_create/list/delete_broadcast` mutate de la `withAuthenticatedUser` → **`withAdminUser`** (get_pending/dismiss rămân user-level).
- `electron/ipc/demoSeed.ts`: `seed_demo_step6` + `clear_demo_step6` → **`withAdminUser`** (status rămâne read).
- Audit log neschimbat (service-ul rulează identic, doar gardul e acum explicit la IPC).

### E. Audit log gaps — acțiuni adăugate în audit (hard-delete / permisiuni / financiar)
| Fișier:metodă | Acțiune auditată |
|---|---|
| `userService.updateUserPages` | `UPDATE user_permissions` (before/after custom_pages) — **privilegii** |
| `threeWayMatchService.deleteSupplierInvoice` | `DELETE supplier_invoice` |
| `threeWayMatchService.approveSupplierInvoice` | `APPROVE supplier_invoice` — **aprobare financiară** |
| `threeWayMatchService.rejectSupplierInvoice` | `REJECT supplier_invoice` (+reason) |
| `salesService.delete` | `DELETE sales_lead` (hard delete, fără urmă domeniu) |
| `quotationService.delete` | `DELETE quotation` |
| `rfqService.delete` | `DELETE rfq` |
| `goodsReceiptService.delete` | `DELETE goods_receipt` |

(`_user`→`user` unde param-ul era ignorat; toate apelurile IPC pasau deja user-ul.)

### Amânate (motiv) / report-only batch 2
- **Audit „self-service"** (personal tasks/calendar, dashboard config): intenționat NEadăugat — zgomot, nu gap de conformitate.

## Batch 3 — audit plumbing rămas (E) + N+1 (D)

### E (continuare) — threading `user` + audit pe hard-delete-urile fără param user ✅
| Fișier:metodă | Schimbare |
|---|---|
| `deplasariService.delete` | semnătură `(db, id)` → `(db, user, id)` + `logAuditEvent('delete','deplasare')`; IPC `delete_deplasare` pasează `user` |
| `projectService.deleteClient` | semnătură `(db, id)` → `(db, user, id)` + `logAudit('delete','client')`; IPC `delete_client` pasează `user` |
(Verificat: singurele call-site-uri sunt handler-ele IPC — ambele actualizate.)

### D — N+1 eliminat în `quotationService.list` ✅
- `rowToQuotation` despărțit în `mapQuotationRow` (doar scalari) + atașare copii. `list` colecta liniile/evenimentele prin `loadLines`+`loadEvents` **per rând** (2N+1 query-uri).
- Acum: `mapQuotationRow` pe toate rândurile, apoi **2 query-uri batch** (`batchLoadLines`/`batchLoadEvents` cu `WHERE quotation_id IN (...)`), grupate în JS prin `Map`. `get`/`getByToken` (un singur rând) rămân pe `rowToQuotation` neschimbat.
- Test nou (`quotationService.tx.test.ts`): `list()` returnează liniile corecte per ofertă, fără cross-contamination; `get()` single-row tot atașează copiii. 4/4 pass.

## Clase scanate — curate (verificat, fără bug-uri)

- `JSON.parse` neguardat pe input DB: doar #1 real; restul (chat, checklist, deplasari, pieces, fisaTemplates, notifications, permissions) sunt deja în try/catch.
- Memory leaks (setInterval/addEventListener/EventSource în `src/pages` + `src/hooks`): **0** — toate `useEffect`-urile returnează cleanup; store-urile (dashboard/handoff) au polling ref-counted cu `clearInterval`.
- Împărțire la zero / `NaN` în metrici (search, serviceTicket, checklist): guardate.
- Leak de erori tehnice la client prin HTTP boundary: `CommandError` → mesaj prietenos; altele → generic, fără SQL.

## Detalii

### Bug #1 — reportsService.listPresets: parse neguardat per-rând 🟠
- **Loc:** `electron/services/reportsService.ts` (`listPresets`).
- **Reproducere:** un rând în `report_presets` cu `config` invalid (edit manual DB, scriere parțială, schemă veche) → `JSON.parse` aruncă → tot endpoint-ul `list_report_presets` returnează 500 cu eroare tehnică; userul nu mai vede niciun preset salvat.
- **Fix:** parse per-rând în try/catch cu fallback la `{ source }` (config minim valid) → preset-ul rămâne vizibil și poate fi re-salvat/șters; restul listei nu mai e afectat de un rând corupt. UPGRADE, fără schimbare de tip public.
