node_modules/
dist/
dist-electron/
dist-server/
data/
release/
*.db
.claude/
playwright-report/
test-results/
coverage/
ai-service/target/
ai-service/target-linux/
ai-service/target-win/
ai-service/dist/
# Real config (with secrets) — track only the .example template. Existing
# checkouts must `git rm --cached ai-service/config.toml` after updating.
ai-service/config.toml

# Desktop build artifacts (large)
dist-installer/
public/downloads/*.exe
public/support/*.exe
public/support/RustDesk2.toml
build/icon.png

# ── Extra safety: keys/env + large binaries + local backups (added for upload) ──
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.jks
*.p12
*.pfx
*.minisign
.dbkey
*.apk
*.aab
*.tsbuildinfo
*.log
.restart-marker
_comments_backup/
_codex_current_ui_backup_*/
_filterbar_backup_*/
_upload-bundles/

# ── GitHub upload exclusions (large data / DB backups / editor) ──
backups/
*.zip
.vscode/
.idea/
.update-staging/
.env.tauri

# ── Runtime data, preview harnesses, dev clutter (2026-07-02 upload) ──
.preview-data/
.demo-preview-data/
.cursor/
preview-shots/
screenshots/
public/shots/
logs/
ds-bundle/
tenants/*/data/
deploy/rustdesk/bin/
deploy/rustdesk/data/
**/.keys/
tools/license-generator/out/
tools/license-generator/registry.json
._mkzip.cjs
build-out.txt
tsc-now.txt
*.patch
*.lic

# installer build artifacts
updates/
src-tauri/nsis/*.exe
