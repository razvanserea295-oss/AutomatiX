// Collapsed into the redesign design-system (enterprise-overhaul-audit T1.1).
// This module is now a thin re-export so all legacy importers pick up the
// single canonical implementation in src/redesign/ui — one place to skin.
export * from '@/redesign/ui/Button';
export { default } from '@/redesign/ui/Button';
